#include "macierze.h"

using namespace std;

int main()
{
    //setlocale(LC_ALL,"polish");
    //srand(time(NULL));
    //od tego miejsca kopiowac do funkcji menu()

    //funkcje menu() wstawic tutaj
    menu(M1, M2);
    return 0;
}
